kind: role
version: v7
metadata:
  name: base
  description: "Base role: no standing privileges, request-only access"
spec:
  allow: {}
  deny: {}
  options:
    max_session_ttl: 8h
    lock: best_effort
