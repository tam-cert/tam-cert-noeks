---
- import_playbook: k8s-setup.yaml       # 1 - containerd, kubeadm, kubelet, kubectl
- import_playbook: k8s-master.yaml      # 2 - kubeadm init, Calico CNI, kubeconfig
- import_playbook: k8s-workers.yaml     # 3 - dynamic kubeadm join
- import_playbook: postgres.yaml        # 4 - in-cluster PostgreSQL pod + cert generation
- import_playbook: teleport.yaml        # 5 - Teleport Enterprise Helm install
- import_playbook: access-graph.yaml    # 6 - Teleport Identity Security / Access Graph
- import_playbook: teleport-oidc.yaml   # 7 - AWS OIDC integration resource
- import_playbook: teleport-sso.yaml    # 8 - Okta SAML connector (bootstrap only)
- import_playbook: teleport-rbac.yaml   # 9 - Machine ID bot + rbac-manager role + join token
- import_playbook: teleport-node.yaml   # 10 - AWS IAM join token for ssh-node-1
- import_playbook: argocd.yaml          # 11 - ArgoCD + Teleport app + RBAC sync Application
